# 运行链路与边界

```text
个人客户端（个人网关密钥；工具在自己的电脑执行）
  → HTTPS relay.yanero.top:443
  → VPS nginx SNI 分流至 127.0.0.1:9444 并终止 TLS
  → FastAPI 127.0.0.1:18021
  → 身份检查 → SQLite 预扣预算 → 公平队列
  → 官方 app-server stdio JSON-RPC
  → 订阅账号的官方模型服务
  → app-server 通知 → SSE/JSON → 个人客户端
```

Cloudflare 只负责 DNS，记录为 DNS-only。客户端连接此 VPS，模型流量由此 VPS 发出。官方仍可观察账号、请求内容及行为；此结构不提供“隐藏共享”的保证，不伪造官方客户端身份。

本文描述已发布的 0.3.0 行为。本轮验收范围和容量证据见 REPAIR_REPORT.md；未承诺上游订阅的 6 路可用能力。

## app-server 的角色与连接标识

app-server 是 OpenAI 开发、运行在本项目 VPS 上的本地程序。本网关负责用户鉴权、预算、排队和管理页面；app-server 负责订阅认证、会话及与 OpenAI 云端的模型通信；模型计算发生在 OpenAI 云端。网关通过本机 stdio JSON-RPC 调用 app-server。

初始化时本网关向本机 app-server 提交 `clientInfo={name:"codex_relay",title:"Codex Relay",version:"0.3.0"}`。初始化握手是协议要求，具体名称由集成自行选择，不是四人数量或设备清单。官方文档说明 `clientInfo.name` 用于识别集成，app-server 会提供其向上游发送的 User-Agent；应把名称视为官方可见。尚未抓包核验准确 User-Agent，以及 title、自定义 version 是否原样传出。此处 0.3.0 是代码中的集成版本，区别于官方二进制版本 0.153.2。

只讨论协议与网络元数据：上游可见 VPS 出口、认证账号、请求时间/并发/模型/用量及 app-server 发送的标识。网关不透传个人客户端的 User-Agent 或来源 IP，也不采集、转发个人设备名称、MAC 地址和硬件序列号；个人网关密钥、用户编号和预算用于本地鉴权/记账，不额外加入上游模型请求或元数据。用户自行写入任务正文、工具说明或上下文的内容仍会随请求发送。此结论仅覆盖本网关模型链路，不代表客户端其他独立连接经过 VPS，也不是上游遥测字段的完整抓包清单。

参考：[OpenAI app-server 初始化说明](https://learn.chatgpt.com/docs/app-server#initialization)。

## 客户端工具

图片理解复用对话模型：用户的 `input_image` 转为 `turn/start` 的图片输入，直接附图保留 `detail`。客户端工具结果允许文字/图片混合数组，转为原生 `inputText` / `inputImage`，保持调用关联和用户隔离。工具图片使用原生默认清晰度。看图计入上游对话总用量，不另加估算图片 token；实现范围见 [VISION.md](VISION.md)。

客户端在 `tools` 中声明函数。网关映射为 `relay_tool_N` 动态工具，保留对外名称。模型调用工具后，网关向客户端返回 `function_call`；官方 turn 暂停等待工具结果。客户端在自己的运行环境执行工具，再提交 `function_call_output`。网关恢复同一 turn，随后继续返回结果。

账号凭据、SQLite、服务器文件不通过工具暴露。线程使用 `environments: []` 禁止服务器执行环境；关闭 orchestrator skills/MCP、插件及其他原生功能。模型目录显式选用直接工具模式并关闭原生 shell、apply_patch 和多代理元数据。对外只接受目录中的模型，避免未知模型绕过已验证配置。模型 ID 不做替换。

模型目录来自 OpenAI 官方仓库的模型元数据，经本项目精简为客户端工具用途；保留模型能力参数，替换运行指令及服务器工具选择配置。目录是固定快照，升级二进制或目录后必须重新执行真实二进制模拟测试。

## 配额

- 初始化默认每人预算 500,000,000 token，可由管理员修改；2026-09-05 最近一次部署核验为每人 400,000,000，实际值以管理页面为准。用量持续累计，无自动重置，仅由管理员手动归零。
- 记账口径：官方 `inputTokens + outputTokens`；缓存输入已经包含在输入中，推理输出已包含在输出中，均不重复相加。
- 接入时按文本和工具定义的 UTF-8 字节数估算输入，加上每张图片 65,536 token 及输出预留 8192。直接附图、工具结果和恢复的完整历史图片均计入图片数量，Base64 字节不直接当成模型 token；这是当前图片模型边界下的保守准入估算，不保证未知未来模型的上限，最终只按上游实耗结算。SQLite 事务阻止同时通过检查的请求重复占用剩余额度。
- SQLite 的 `reserved` 保留原预留总额，避免改变旧数据库语义；每行未消费预留为 `max(0, reserved - charged)`。用量接口的 `held` 汇总该值，请求列表的 `reserved` 也展示该值，已消费部分不再重复占用余额。
- 官方用量上报后持久化检查点，重复累计通知不重复计费；最终结算释放剩余预留。断线、取消或重启时保留已知消费，只将尚未确认的预留保留为“待核对”，不凭估算补造消费。
- 部分 app-server 用量通知延迟到工具返回之后。后续累计用量包含此前工具段时，自动清除这些段的待核对预扣，并由续接请求承接累计用量；因此按 HTTP 请求查看时，某些工具段本身为 0，后续段包含其用量。
- 新请求须有足够余额覆盖自己的预留。已准入请求检查用量时可继续使用自身未消费预留，但仍需保护其他请求的预留；已用量加其他请求预留恰好等于预算时允许完成，超过预算才中断。官方不能通过此接口硬限制 `max_output_tokens`，且用量上报并非逐 token，因此一次模型响应可能造成超额。这是准入及用量更新点上的限制，不是逐 token 的精确硬切断。
- 内部 token 预算与官方 5 小时/每周额度不等价。管理员页面单独显示官方可用的限额窗口信息。

## 批量管理与上游观测

管理员重置通过记录当前已计费用量的免计偏移实现，历史 `charged` 不变；在途请求未来新增的用量继续占用额度。预算更新和重置均通过 SQLite 事务执行，操作日志保存在 `admin_actions`。任意时间范围统计按请求开始时间筛选原始已结算量，不扣除重置偏移。

上游采样使用官方 `account/read` 与 `account/rateLimits/read`。按账号与套餐哈希、额度池、窗口分别保存样本；同一重置时间且百分比单调上升、采样间隔不超过 180 秒时才能串接。主 Codex 额度池以同区间官方用量观测增量除以上游消耗百分点估算比例；检查点和最终重复上报不重复记入观测计数，手动补账不作为新上游观测。额度重置、账号/套餐切换、百分比下降和长时间中断均重建基线。比例至少积累 1 个百分点才展示，其他入口消耗无法归因，会造成估算偏差。

## 并发与隔离

全局最多 6 个正在处理的模型请求；每用户 `concurrent_limit` 默认 1，管理员可逐人设置为整数 1–6，持久化到 SQLite。保存后立即更新内存调度器：提高限额立即调度符合条件的等待者，降低限额让现有请求正常结束，运行数低于新限额后才发放下一槽。四名用户都保持 1 时最多同时运行 4 个请求；不同会话受个人限额与全局 6 槽共同约束。

队列最多 16 个，等待上限 120 秒，按可运行用户轮转。个人已满的队列不会挡住其他用户使用空闲槽；只有必须排队的新请求才检查队列容量。工具等待段暂时释放生成槽位，执行结果到达后重新排队。每用户最多保留 16 个活动或等待工具的会话，创建中的会话也计入名额；工具等待最多 900 秒。

同一活跃会话的续接在校验、消息注入和事件读取期间独占，竞争提交返回 409，不重复消费工具结果。只使用一个会话、不调用子代理的顺序生成流程通常不需要个人并发大于 1；本地工具执行不受该生成并发数约束。上游共享额度、网络延迟和协议子集仍会影响与官方桌面登录的体验差异。

身份只来自个人密钥。历史续接、工具回调和取消请求均检查所属用户；同时提供工具结果与 previous_response_id 时还必须对应同一响应。窗口本身不作为身份，同一用户的不同窗口由各自提交的历史和响应 ID 区分，不会自动续接该用户“最近的会话”。

响应历史持久化到 SQLite 的 response_history 表，默认每个响应自创建起保留 7 天，每用户快照正文总量最多 100 MiB（UTF-8 JSON 字节数，包含图片与工具结果）。超过本人容量时按创建顺序清理本人的旧快照，不清理其他用户；单个快照超出上限则本次响应失败。过期快照在启动后的清理任务及请求处理时删除。限额约束可检索快照正文，不是整个 SQLite 文件的物理大小上限；WAL、空闲页和备份需按运维规则管理。

新响应的历史提交成功后才发送 response.completed。网关重启后，可用同用户的已完成响应 ID 从数据库恢复历史并新建临时上游线程。上游继续使用 ephemeral 会话和空执行环境。完整历史请求不依赖 response ID；同时提供 ID 与完整历史时，仅在前缀完全匹配（忽略响应展示字段）时去重，不能把部分历史猜成完整历史。恢复后的上下文参与预算预扣。

历史已清理但账本能确认属于本人时返回 409 response_history_required；未知或其他用户的 ID 均返回 404 previous_response_not_found。收到 409 后，客户端须移除旧 previous_response_id 并明确重发自己保存的完整历史，不能只发最新一句冒充恢复。升级前未保存的旧历史同样需要客户端重发。

工具等待仍最多 15 分钟，运行中的 turn 和工具 Future 不做持久化恢复。工具回传可附带未改变的历史和新增 user 消息；全部校验通过后先将新消息注入当前线程，再释放工具结果，新增内容同时写入历史。明确的注入拒绝返回 400 并保留等待状态；注入结果不确定时中断该会话，避免隐式重复注入。模型、工具、指令和生成设置在工具等待期间必须保持一致。

工具后的独立压缩请求有单独路径：请求须提供归属和前缀匹配的完整历史、成对的工具调用与结果，并且不声明动态工具。网关校验后结束旧工具等待，在新线程注入已有历史生成摘要，不重新执行客户端工具；普通工具续接仍遵守原生成参数约束。此协议路径的存在不等于已完成真实桌面端自动压缩验收。

重启、超时或已消费的工具续接返回 409 continuation_lost，不自动重跑客户端工具。客户端若选择新建会话恢复，必须提供成对的历史工具调用与已有执行结果，并自行确认哪些操作已经完成；不得盲目重复执行有副作用的命令。

Web 登录密钥只保留在页面内存。用量接口和日志不返回聊天正文，账本不保存明文密钥；新增 response_history 表会保存提示词、模型回复、图片、公开推理摘要/加密推理项和工具结果。数据库与备份因此包含聊天内容，需限制服务账号/root 访问并执行保留期清理。官方登录凭据仍只在专用服务账号的 CODEX_HOME 中保存。

## 支持范围

支持文本、内嵌 PNG/JPEG/WebP、system/developer/user/assistant 消息、函数工具、客户端 custom 工具桥接、JSON Schema 输出、流式文本、公开推理摘要及同用户历史续接。没有文本 delta 时以最终正文补全；部分 delta 补余下正文，已完整发送的正文不重复追加。

不接受服务器文件路径、远程图片 URL、服务器内建工具、任意上游地址、未知模型、`max_output_tokens`、强制特定工具选择。经典格式带顶层工具时，`parallel_tool_calls=false` 无法由当前 app-server 桥接保证，明确返回 400，不能与用户生成并发混为一谈。`custom` 工具的语法描述通过普通函数参数桥接，不能保证上游执行自定义文法约束。文件上传、音频、后台 Responses 任务、完整 Assistants/Chat Completions API 不在本版本范围内。

`text.verbosity` 接受 `low`、`medium`、`high`，映射到上游线程的 `model_verbosity`；`reasoning.summary` 接受 `auto`、`concise`、`detailed`、`none`，非法枚举返回 400。公开摘要是否产生取决于上游，网关不返回原始非公开推理正文。

`store` 不控制本中转的 7 天历史恢复存储；该恢复机制独立于上游 Responses 的 store 语义。`metadata` 仅回显给调用方。`client_metadata`、`include`、`stream_options`、缓存提示字段、`user` 及安全标识字段作为客户端兼容字段接收，不映射为自定义上游身份，也不实现其各项上游可选语义；实际模型缓存由官方 app-server 管理。客户端应按本项目 Responses 子集接入，不应假设所有 SDK 可选参数都受支持。

排队满与排队超时分别返回 `429 queue_full`、`429 queue_timeout`。取消覆盖排队、准备、生成和等待工具；原响应的终态为 `cancelled`，流式通过 `response.failed` 事件传递该终态及 `cancelled` 或 `user_disabled` 原因，不使用自创的 `response.cancelled` 事件。


Codex 新版 Responses Lite 兼容：接受首个 developer additional_tools 包装，将其中工具声明经过原有客户端工具校验与别名映射后使用，不把原名工具重复暴露给上游。Codex 在该协议自动发送 parallel_tool_calls=false；网关按协议适配，不能将此固定值视作用户要求工具串行。经典格式带顶层 tools 且 false 仍明确拒绝。此区别不影响全局及个人生成并发限制。
