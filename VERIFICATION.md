# 部署验证记录 · 2026-09-06

当前版本为 0.3.0，本地和 VPS 各 159 项测试通过。本轮已上线 repair-20260906；完整记录见 [REPAIR_REPORT.md](REPAIR_REPORT.md)。额度仅手动归零，默认接入示例为 `gpt-6-astra / medium`。以下按发布阶段保留历史验证结果，早期的每周重置和 5 亿线上预算不代表当前配置。

## 用户隔离与持久续接升级

- 完整 pytest 47 项通过，编译检查通过。新增覆盖数据库迁移与重开、用户间容量隔离、同用户双会话、过期与容量错误、历史去重、完整上下文预扣、工具续接混入新消息、所有权及取消隔离、持久化失败、失效工具状态不重放、工具原名与内部别名说明。
- VPS 官方 app-server 对全部 8 个开放模型通过模拟上游协议测试：`gpt-6-astra`、`gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`、`gpt-5.5`、`gpt-5.4`、`gpt-5.4-mini`、`gpt-5.2`。验证实际进程/数据库重启后续接、跨用户拒绝、工具回传时的新消息到达上游；上游仅收到客户端声明的工具。
- 本机桌面版内置 `codex-cli 0.153.0` 作为真实客户端，通过 HTTP 网关连接真实 app-server 与模拟模型：创建 A/B 两个独立任务、重启网关后恢复 A，确认不带入 B；实际执行客户端命令工具并回传成功。脚本：`python -B verify_client_protocol.py <桌面版 codex.exe 路径>`。成功标记为 `REAL_CLIENT_TWO_WINDOWS_AND_RESTART_PASSED`、`REAL_CLIENT_TOOL_ROUND_TRIP_PASSED`。
- 客户端发送的 `client_metadata` 作为可选对象接受，不参与身份或会话匹配。身份只取个人密钥对应的用户，续接取明确的响应 ID 或尚存活的工具调用 ID。
- 历史保存到 SQLite，每条最长 7 天、每人合计 100 MiB，达到容量会提前淘汰该用户最旧快照。待回传工具的执行状态仍为内存状态，重启后明确报错，不自动重做工具。升级前仅存于旧进程内存的历史无法迁移。
- 上述真实客户端测试覆盖桌面版内置引擎协议，并非对桌面 GUI 所有功能或两个物理窗口的完整验收。
- 已上线 `/opt/codex-relay-v2/releases/context-continuity-final-20260906`。两次版本切换前后的用户、密钥、预算及原始用量账本摘要完全相同；迁移仅追加历史表与索引。最终切换前备份：`/opt/codex-relay-backups/context-continuity-20260905-181515`；首次迁移前备份：`/opt/codex-relay-backups/context-continuity-20260905-181012`（目录时间为 UTC）。
- 公网 HTTPS + 真实 `gpt-6-astra / low` 验收通过：用户 2 引用用户 1 的响应返回 404；同用户创建 A/B 会话，重启生产服务后只提交 A 的响应 ID 和新问题，正确恢复 A；SSE 工具调用、回传结果附带新消息，模型回复包含两个独立测试标记；所有 6 个测试响应快照归属用户 1。
- 首次真实工具请求暴露既有工具改名问题，模型找不到提示中的原工具名。最终版本在工具描述中明确原名和内部别名后，以相同调用要求复验通过。总计 6 个真实请求计入用户 1 共 1,379 token，预扣和待核对记录均为 0；未重置用量。服务器验收明细保存在 `/root/context-continuity-verification.json`，仅 root 可读。

## 图片理解升级（2026-09-06）

已上线 `vision-20260906`。59 项本地测试、桌面自带 Codex 执行程序的附图和内置看图工具协议测试、VPS 官方 0.153.2 配合模拟上游及现有订阅的识图/历史追问/图片工具回传验证均通过。隔离订阅验证使用测试账本记入 1,392 token；上线后的公网 2.8 MB 附图及追问实际给用户 1 记账 445 token，无重复扣费或剩余预扣。完整范围和限制见 [VISION.md](VISION.md)。

## 首次部署验证（历史）

- 本地 pytest：18 项通过。覆盖身份/管理员隔离、密钥轮换、停用、并发预扣、缓存不重复计费、账本重启恢复、延迟用量自动结算、上海周重置、公平排队、队列上限/超时/取消、工具所有权、历史续接、取消唤醒、SSE、推理项字段过滤和不支持输入的拒绝。
- VPS 官方 `codex-app-server 0.153.2` 连接本地模拟上游：`gpt-5.6-sol`、`gpt-6-astra` 的文本、客户端函数往返和官方累计用量验证通过。
- `gpt-6-astra` 的真实二进制历史注入与 `previous_response_id` 测试通过。
- 模拟请求实测上游工具列表：纯文本为空；声明函数时只包含对应 `relay_tool_0`。模型运行目录/原生工具开关不能单独依赖旧 `features.apply_patch_freeform=false`，已使用空执行环境、关闭 orchestrator 工具及固定模型目录解决。
- 公网 DNS 和 HTTPS：`https://relay.yanero.top/healthz` 返回 `ok:true`，有效 Let's Encrypt 证书，自动续期已设置。
- 生产服务以专用 `codex-relay` 用户运行，只监听本机 `18021`；nginx SNI 分流新增独立域名入口。已有 nginx、Sub2API 服务检查为 active。
- 管理员/四名用户线上鉴权检查通过；每名用户预算均为 500,000,000 token。
- OpenAI 设备码登录完成并在服务重启后保持有效。
- 公网真实请求省略 model 字段，默认 `gpt-6-astra` 返回 `RELAY_OK`：官方 165 token，用户 1 扣账 165，预扣归零。
- 公网真实 SSE 函数调用返回 `relay_echo`，客户端回传 `round-trip-ok` 后模型正常回复：累计 490 token，用户 2 扣账 490，预扣和待核对记录归零。
- 已用浏览器检查部署后的登录页面显示。

## 管理员升级验证

- 本地完整 pytest：30 项通过。新增覆盖批量预算及停用用户、历史保留、在途请求重置后继续扣账、时间范围边界、旧账本迁移幂等、管理员接口隔离、采样比例及重新建立基线、手动补账不污染观测计数。
- JavaScript 语法检查通过。浏览器在独立模拟数据环境验证四人批量改预算、确认重置、历史统计保留及自选时间查询，截图检查布局正常。
- 生产升级完成，公网健康状态正常、新页面脚本可访问；新统计/采样接口拒绝普通用户。四人预算仍均为 500,000,000；用户 1、2 已用量为 165、490，历史合计 655，生产批量操作日志为空。
- 无需打开页面或手动触发，后台已产生相隔约 60 秒的两批样本（3 个窗口共 6 行），确认定时采样生效。首次核验主 Codex 窗口剩余 75%；比例等待后续实际消耗数据，不进行刻意消耗额度的验证。

## 取消周重置验证

- 完整 pytest 30 项通过；用跨北京时间周一零点、跨 8 周、重开数据库后继续限额的回归测试替换原周重置测试。历史周记录与新记录一并计入；跨周后手动重置仍保留历史及预扣，并从新用量继续扣账。
- 两个前端脚本通过 JavaScript 语法检查；TXT 中 Codex 配置经 TOML 解析校验，默认 `gpt-6-astra` / `medium`。
- 升级前重新读取生产状态：管理员已将四人预算设为 400,000,000，当前已用/预扣均为 0；本次以该状态为准保留，不恢复先前的 500,000,000。
- 已上线 `/opt/codex-relay-v2/releases/manual-quota-20260905`；线上核验新接口返回 `reset_policy=manual`、`resets_at=null`，发布前后的用户预算、用量、预扣和原始请求账本完全一致。服务与 nginx 均正常运行，公开 TXT 下载及页面入口可用。备份：`/opt/codex-relay-backups/manual-quota-20260905-135245`。

## 接口兼容性边界

以上验证不代表所有第三方客户端的所有可选参数都兼容。此版本使用 Responses 子集。额度为内部准入/结算规则，不能保证生成中逐 token 硬切断，也不能保证服务方无法识别账号共享。完整边界见 DESIGN.md。

Codex 桌面版尚未完成全部功能端到端实测。本轮用真实引擎连接模拟模型，核对了请求体与请求头中的管理标记边界，未发现额外转发网关密钥、内部用户或预算。该证据不覆盖生产所有遥测，也不能证明共享不可识别。
