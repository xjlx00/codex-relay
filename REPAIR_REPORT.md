# 修复及上线验收 · 2026-09-06

已发布版本 **0.3.0**，生产目录 `/opt/codex-relay-v2/releases/repair-20260906`，公网入口 https://relay.yanero.top/ 。本次使用原有订阅登录，未创建或购买 API 密钥。

## 已完成

- 全局生成并发固定 6；四名用户迁移后均为 1。管理员可在用户卡填写 1–6 并“保存并发”，即时生效、重启保留。上调立即放行，下调不打断已经生成的请求。工具等待不占生成槽。
- 修复重复预扣、额度恰好耗尽时的误中断、断线及重启的已知消费保留、延迟工具计量和重复检查点。数据库 reserved 保持原值，界面及预算仅扣 `max(0, reserved - charged)`，无需改写旧账本。
- 同一工具会话续接互斥，防止重复提交；新建会话提前占位，防止并发穿透 16 个会话上限。
- 修复工具之后自动压缩上下文、取消/停用在排队与准备阶段的竞态、无增量及部分增量时的最终正文、公开摘要流式输出。排队、上游超时、限流和过载分别返回明确错误。
- 修复 RPC 超长日志堵塞、订阅溢出清理及返回结果时的取消竞态。
- 兼容新 Codex Responses Lite 的 `additional_tools` 开头声明和固定 `parallel_tool_calls=false`。经典格式带顶层工具的 false 仍明确拒绝，不能保证工具串行；此限制与个人生成并发分别管理。
- 图片预留改为每张 65,536 token 的保守估算，不再把 Base64 文件字节当 token；最终只按上游用量结算。补齐 verbosity 支持、参数校验、接入诊断及真实历史保留说明。

## 验收证据

| 验收 | 结果 |
| --- | --- |
| 本地及 VPS 完整 pytest | 各 159 项通过；原有覆盖保留 |
| 官方 CLI 0.153.4 及真实 app-server，模拟模型 | 经典格式、Responses Lite、默认 Code Mode 均通过；工具执行后自动压缩、继续调用工具，前一命令未重跑 |
| 图片客户端 | 直接附图、真实 view_image 和图片工具回传通过 |
| 后台页面 | 保存用户并发 3 成功，7 被拒绝；运行/上限/排队显示正常 |
| 服务器容量 | 默认四人峰值 4，调整为 3/2/1 后峰值 6；额外请求排队，无串响应或串账 |
| 容量负载 | 六路各约 2 MiB 有效 PNG 加 64 KiB 历史文本，单请求约 2.87 MB；12 请求模拟实耗合计 1320 token |
| 内存 | 在配置 MemoryMax=900M、TasksMax=256 的隔离服务中通过；Python 与 app-server 合计采样 RSS 峰值 498,520,064 字节，约 475.4 MiB |
| 真实订阅 | 经典格式和最终 Lite 格式各验证一次客户端工具往返；每轮 517 token，使用隔离测试账本，不改原用户用量 |
| 发布后公网 | HTTPS/SSE、连续事件序号、Lite 请求和实际结算通过；用户 1 本次验收计 163 token，剩余预扣 0 |
| 发布完整性 | 本地与受检服务器 32 个文件哈希一致；切换前后原用户、密钥、预算、请求及累计账本摘要一致 |

明细见 `REPAIR_CLIENT_REPORT.json`、`REPAIR_CAPACITY_REPORT.json`、`REPAIR_LIVE_REPORT.json`、`REPAIR_PUBLIC_REPORT.json` 和 `REPAIR_VERIFIED.json`。发布后仅进一步澄清接入 TXT 文案，哈希记录同步更新，业务代码未变。

## 上线与回滚

- 上线时间：2026-09-06，北京时间约 09:14。
- 前一版本：`/opt/codex-relay-v2/releases/vision-20260906`。
- 完整 SQLite 备份：`/opt/codex-relay-backups/repair-20260906-011354/relay.sqlite3`，仅 root 可读，目录时间为 UTC。
- `upgrade_repairs.py` 在空闲窗口检查旧版本与受检文件、停服务再次确认、备份完整数据库、切换链接、检查健康和账本一致性；失败自动恢复旧代码。
- 正常代码回滚保留当前数据库；新增并发列和原 reserved 语义兼容旧代码。不能用旧数据库备份覆盖上线后的新消费。
- 发布后服务 active、NRestarts=0，原生产上游登录和用户密钥继续使用。

## 明确边界

6 路容量验收使用本地模拟模型，不保证官方订阅始终允许 6 路，也未覆盖六份 100 MiB 历史的最坏内存负载。RSS 为采样值，不是整个 cgroup 峰值。

真实默认 Code Mode 的工具与压缩路径已测，尚未覆盖桌面 GUI 的全部功能、插件、所有模型组合或所有 Responses 可选参数。单会话且不用子代理时，个人并发 1 通常不增加模型轮次之间的等待；整体速度仍受网络、共享额度和上游负载影响。

已验证网关不会额外把网关密钥、内部用户及预算管理标记发到模型请求体或请求头中。保留真实集成标识。上游仍能看到认证账号、任务内容、请求及并发特征，无法承诺 ChatGPT/OpenAI 不识别共享。

连续对话历史含正文、图片和工具结果，逻辑保留最多 7 天、每人 100 MiB，容量满会提前清理；备份和 SQLite 物理残留不属于立即擦除保证。
